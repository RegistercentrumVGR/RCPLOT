% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highcharts.R
\name{forest_plot_highcharts}
\alias{forest_plot_highcharts}
\title{Highchart Forest plot}
\usage{
forest_plot_highcharts(
  df,
  x_var,
  y_var,
  y_lower,
  y_upper,
  color_var = NULL,
  color_var_order = NULL,
  legend_title = NULL,
  title = "",
  y_lim = NULL,
  y_breaks = NULL,
  log_scale = FALSE,
  proportion = FALSE,
  scale_percentage = TRUE,
  reference_line = NULL,
  other_vars = NULL,
  x_lab = NULL,
  y_lab = NULL,
  horizontal = TRUE,
  arrange_by = NULL,
  arrange_desc = TRUE,
  x_var_order = NULL,
  marker_size = 4,
  marker_symbol = "diamond",
  plot_height = NULL,
  text_size = NULL,
  n_decimals = 0,
  facet_by = NULL
)
}
\arguments{
\item{df}{data}

\item{x_var}{the variable with the categories/subgroups to display, one
point is drawn per unique value}

\item{y_var}{the point estimate variable}

\item{y_lower}{the lower bound of the confidence interval}

\item{y_upper}{the upper bound of the confidence interval}

\item{color_var}{the name of the color variable, draws a separate
point/confidence interval series per level, useful for e.g. showing
several models or timepoints side by side within each subgroup}

\item{color_var_order}{what order \code{color_var} should be displayed in, can
alternatively be \code{auto_character} or \code{auto_numeric} to automatically sort
the levels}

\item{legend_title}{title of the legend}

\item{title}{title of the graph}

\item{y_lim}{limits of the estimate-axis, if \code{NULL} and \code{proportion} is
\code{TRUE}, will be set to \code{c(0, 100)}}

\item{y_breaks}{breaks of the estimate-axis}

\item{log_scale}{if the estimate-axis should be displayed on a logarithmic
scale, useful for e.g. odds ratios and hazard ratios}

\item{proportion}{if the estimate and confidence interval are proportions,
useful for e.g. displaying proportions or survival probabilities with
confidence intervals across subgroups}

\item{scale_percentage}{if proportions should be re-scaled to be
percentages}

\item{reference_line}{reference line to draw across the estimate-axis, e.g.
\code{1} for odds/hazard ratios or \code{0} for mean differences}

\item{other_vars}{other variables to include in the tooltip, should be a
named list where the name will be the key in the tooltip}

\item{x_lab}{labels on the category-axis}

\item{y_lab}{labels on the estimate-axis}

\item{horizontal}{whether categories should be displayed on the vertical
axis with the estimate on the horizontal axis, which is the conventional
way to display a forest plot}

\item{arrange_by}{column to sort \code{x_var} by}

\item{arrange_desc}{to arrange descending}

\item{x_var_order}{the order the categories should be displayed in}

\item{marker_size}{size of the point estimate marker}

\item{marker_symbol}{shape of the point estimate marker}

\item{plot_height}{height of plot, value is in pixels}

\item{text_size}{size of text, will be interperted as pixels}

\item{n_decimals}{number of decimals to round numbers to}

\item{facet_by}{variable in \code{df} with at most 2 unique values to facet by;
if supplied the return value is a named list of plots, one per facet level}
}
\value{
highcharts config, or a named list of configs when \code{facet_by} is set
}
\description{
creates a forest plot in highchart, outputs config for highchart. Draws a
point estimate together with a confidence interval for each level of
\code{x_var}, useful for e.g. displaying odds ratios or hazard ratios with
confidence intervals across subgroups.
}
