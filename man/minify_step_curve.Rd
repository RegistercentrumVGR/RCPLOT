% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highcharts.R
\name{minify_step_curve}
\alias{minify_step_curve}
\title{Minify a right-continuous step curve for plotting}
\usage{
minify_step_curve(df, x_var, y_var, group_vars, n_decimals, scale_factor = 1)
}
\arguments{
\item{df}{data}

\item{x_var}{x-axis variable, used to order rows within a group}

\item{y_var}{y-axis variable}

\item{group_vars}{variables identifying a series, or \code{NULL} for a single
series}

\item{n_decimals}{number of decimals \code{y_var} is rounded to before
comparing rows}

\item{scale_factor}{factor \code{y_var} is multiplied by before rounding,
matching the scaling \code{make_series()} applies (e.g. \code{100} when
\code{proportion} and \code{scale_percentage} are both \code{TRUE}) so that rows are
only merged when they'd actually round to the same displayed value}
}
\value{
\code{df}, with redundant rows removed
}
\description{
A right-step curve (e.g. a Kaplan-Meier curve) only visually changes at
the rows where the (rounded) \code{y_var} value differs from the previous row,
so intermediate rows that repeat the same rounded value can be dropped
without changing how the curve is rendered. This keeps the first row and
last row of each \code{group_vars} group, plus every row where the rounded
\code{y_var} differs from the previous row within that group.
}
